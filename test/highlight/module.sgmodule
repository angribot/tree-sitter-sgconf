#!name=Highlight %hostname% module
# <- keyword
#       ^ string
#                 ^ variable.parameter
#!desc=Routes %hostname% with fallback %fallback%
# <- keyword
#       ^ string
#              ^ variable.parameter
#                                       ^ variable.parameter
#!system=mac
# <- keyword
#         ^ constant.builtin
#!arguments=hostname=service.example&fallback=backup.example
# <- keyword
#           ^ variable.parameter
#                   ^ punctuation.delimiter
[General]
# <- type
always-real-ip = %APPEND% %hostname%
# <- property
#                ^ operator
#                         ^ variable.parameter
